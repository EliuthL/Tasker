// Disable button after click and submit form
function submit(button, myForm) {
    button.disabled = true;
    document.getElementById(myForm).submit();
}

function disablebutton(button) {
    button.disabled = true;
}

    